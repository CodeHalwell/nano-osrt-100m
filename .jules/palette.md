## 2024-04-20 - Adding autofocus to inputs and proper chatbot message formatting
**Learning:** Adding `autofocus=True` to Gradio `Textbox` elements improves immediate user interaction by removing an extra click to focus. Also, setting `type="messages"` for `gr.Chatbot` effectively formats history properly.
**Action:** Use these properties when developing or updating Gradio chat UIs.
